# TauriLivePlayer 開発計画書

## 目次

1. [プロジェクト概要](#プロジェクト概要)
2. [背景・動機](#背景動機)
3. [要件](#要件)
4. [技術選定](#技術選定)
5. [システムアーキテクチャ](#システムアーキテクチャ)
6. [データ構造](#データ構造)
7. [開発フェーズ](#開発フェーズ)
8. [マイルストーン](#マイルストーン)
9. [リスクと対策](#リスクと対策)
10. [付録: 技術詳細](#付録-技術詳細)

---

## プロジェクト概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | TauriLivePlayer |
| 目的 | ライブイベント向けタイムコード同期映像再生システム |
| 技術スタック | Tauri 2.0 + React + TypeScript + GStreamer |
| 対象OS | Windows / macOS / Linux |
| 開発期間 | 約16週間（4ヶ月） |

---

## 背景・動機

### なぜ作るのか

1. **TouchDesignerのコスト問題**
   - Pro ライセンス: $2,200 USD
   - 使う機能は全体のごく一部（動画再生、TC同期、出力程度）

2. **既存ツールの不足**
   - NDI + タイムコードチェイス機能を持つフリーソフトが存在しない

3. **Rustを書きたい**
   - 長時間稼働に耐える信頼性
   - パフォーマンスとメモリ安全性の両立

---

## 要件

### 機能要件

| 優先度 | 要件 | 説明 |
|--------|------|------|
| **必須** | Cueベース再生 | 1つのCueで複数メディア（音声×1 + 映像×4など）を同期再生 |
| **必須** | マルチ出力 | Display / NDI / Audio を個別に出力先指定 |
| **必須** | 明るさ調整 | 全出力一括 + 個別調整 |
| **必須** | Windows ASIO | ライブ現場向け低レイテンシ音声出力 |
| 高 | NDI送受信 | High Bandwidth対応 |
| 高 | トラックリストUI | キーボード操作、D&D対応 |
| 中 | NDI|HX | H.264/H.265圧縮送信（低帯域環境用） |
| 中 | マルチスクリーン | 複数映像を1ストリームに合成 / 1ストリームを分割 |
| 中 | TCチェイス | 外部タイムコードに追従 |

### 非機能要件

| 項目 | 要件 |
|------|------|
| 信頼性 | 長時間稼働（8時間+）で安定動作 |
| パフォーマンス | 4K60fps再生、複数ストリーム同時処理 |
| UI | モダンで綺麗なUI（React + shadcn/ui） |
| プラットフォーム | Windows（現場） + macOS（開発） + Linux |

---

## 技術選定

### 採用技術

| カテゴリ | 技術 | 選定理由 |
|---------|------|----------|
| フレームワーク | Tauri 2.0 | 軽量、Rust、WebView UI |
| フロントエンド | React + TypeScript | 開発効率、エコシステム |
| UIライブラリ | shadcn/ui | 綺麗、カスタマイズ性 |
| 映像処理 | GStreamer | パイプライン、NDI対応、実績 |
| Rustバインディング | gstreamer-rs | 公式サポート |
| 状態管理 | Zustand | シンプル、TypeScript |

### プラットフォーム別対応

| コンポーネント | Windows | macOS | Linux |
|---------------|---------|-------|-------|
| Tauri | ◎ WebView2 | ◎ WebKit | ◎ WebKitGTK |
| GStreamer | ◎ | ◎ | ◎ |
| NDIプラグイン | ○ | ○ | ◎ |
| ASIO | ◎ 必須 | - | - |
| HWエンコード | NVENC/QSV/AMF | VideoToolbox | VA-API/NVENC |

---

## システムアーキテクチャ

### 全体構成

```
┌─────────────────────────────────────────────────────────────┐
│  Tauri Application                                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  WebView (React + TypeScript)                         │ │
│  │                                                       │ │
│  │  ┌─────────────┐  ┌─────────────────────────────────┐│ │
│  │  │  Preview    │  │  Cue List                       ││ │
│  │  │  Grid       │  │  - Opening         ▶ Playing   ││ │
│  │  │             │  │  - Song 01           Ready     ││ │
│  │  └─────────────┘  │  - MC Bridge         Pending   ││ │
│  │                   └─────────────────────────────────┘│ │
│  │  [◀] [▶] [■] [▶▶]   🔆 ════●════ 80%                │ │
│  └───────────────────────────────────────────────────────┘ │
│                          │ Tauri Commands (IPC)            │
│                          ↓                                  │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  Rust Backend                                         │ │
│  │                                                       │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌───────────────┐ │ │
│  │  │ CuePlayer   │  │ OutputMgr   │  │ GStreamer     │ │ │
│  │  │             │  │             │  │ Pipeline      │ │ │
│  │  └─────────────┘  └─────────────┘  └───────────────┘ │ │
│  └───────────────────────────────────────────────────────┘ │
│                          │                                  │
└──────────────────────────┼──────────────────────────────────┘
                           ↓
        ┌──────────────────┼──────────────────┐
        ↓                  ↓                  ↓
   ┌─────────┐      ┌─────────────┐     ┌──────────┐
   │Display 1│      │ NDI Output  │     │ ASIO     │
   │(Main)   │      │             │     │ Audio    │
   └─────────┘      └─────────────┘     └──────────┘
```

### GStreamer パイプライン（マルチソース同期）

```
┌─────────────────────────────────────────────────────────────┐
│  GStreamer Pipeline (1つのパイプライン = 自動同期)          │
│                                                             │
│  filesrc(audio.wav) → decode → audioconvert → asiosink     │
│                                                             │
│  filesrc(main.mp4)  → decode → videobalance[main] → disp1  │
│                                     ↑                       │
│  filesrc(side.mp4)  → decode → videobalance[side] → ndi    │
│                                     ↑                       │
│  filesrc(floor.mp4) → decode → videobalance[floor]→ disp2  │
│                                     ↑                       │
│                          各出力ごとに個別の明るさ調整        │
└─────────────────────────────────────────────────────────────┘
```

---

## データ構造

### Cueベース設計

```
Project
├── outputs: OutputTarget[]     # 出力先の定義
└── cues: Cue[]                 # キューのリスト
    └── items: MediaItem[]      # 各キュー内のメディア
```

### TypeScript型定義

```typescript
interface Project {
  id: string;
  name: string;
  outputs: OutputTarget[];
  cues: Cue[];
}

interface OutputTarget {
  id: string;
  name: string;
  type: 'display' | 'ndi' | 'audio';
  
  // 映像出力共通
  brightness?: number;       // 個別明るさ (0-100, 省略時はマスター値を使用)
  
  // Display
  displayIndex?: number;
  fullscreen?: boolean;
  
  // NDI
  ndiName?: string;
  
  // Audio
  audioDriver?: 'auto' | 'asio' | 'wasapi' | 'coreaudio' | 'jack';
  audioDevice?: string;
  audioChannels?: number[];
}

interface Cue {
  id: string;
  name: string;
  items: MediaItem[];
  duration: number;
  loop: boolean;
  autoAdvance: boolean;
}

interface MediaItem {
  id: string;
  type: 'video' | 'audio';
  name: string;
  path: string;
  outputId: string;
  offset?: number;
}
```

### プロジェクトファイル例

```json
{
  "name": "Miku Live 2025",
  "masterBrightness": 80,
  "outputs": [
    { "id": "main", "type": "display", "displayIndex": 1, "brightness": null },
    { "id": "side", "type": "ndi", "ndiName": "LivePlayer_Side", "brightness": 60 },
    { "id": "audio", "type": "audio", "audioDriver": "asio", "audioDevice": "{...}" }
  ],
  "cues": [
    {
      "name": "Opening",
      "items": [
        { "type": "audio", "path": "opening.wav", "outputId": "audio" },
        { "type": "video", "path": "main.mp4", "outputId": "main" },
        { "type": "video", "path": "side.mp4", "outputId": "side" }
      ]
    }
  ]
}
```

- `brightness: null` → Masterに連動
- `brightness: 60` → 個別値（Masterに連動しない）

---

## 開発フェーズ

### 概要

```
Phase 0: 環境構築                    [1週間]
    ↓
Phase 1: 基本再生 (GStreamer)        [2週間]
    ↓
Phase 2: UI実装（Cueベース）          [2週間]
    ↓
Phase 3: マルチ出力ウィンドウ         [2週間]
    ↓
Phase 4a: NDI (High Bandwidth)       [2週間]
    ↓
Phase 4b: NDI|HX (H.264/H.265)       [2週間] ※必要時
    ↓
Phase 5: マルチスクリーン             [2週間]
    ↓
Phase 6: TCチェイス                  [2週間]
    ↓
Phase 7: リリース                    [1週間]

総期間: 約16週間（4ヶ月）
```

---

### Phase 0: 環境構築（1週間）

#### macOS（開発機）
```bash
# Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# GStreamer
brew install gstreamer gst-plugins-base gst-plugins-good gst-plugins-bad

# Node.js + pnpm
brew install node && npm install -g pnpm
```

#### Windows（現場用）
```powershell
# Rust
winget install Rustlang.Rust.MSVC

# GStreamer (MSVC版)
# https://gstreamer.freedesktop.org/download/

# ASIO SDK（必須）
# https://www.steinberg.net/developers/
# → GStreamer ASIOプラグインをビルド
```

#### プロジェクト作成
```bash
pnpm create tauri-app tauri-live-player --template react-ts
cd tauri-live-player
pnpm install
pnpm dlx shadcn@latest init
```

#### 成果物
- [ ] macOS / Windows でビルド成功
- [ ] GStreamer動作確認（`gst-launch-1.0`）
- [ ] Windows: ASIOプラグイン動作確認

---

### Phase 1: 基本再生（2週間）

#### 機能
- ファイル読み込み・再生・停止・シーク
- 明るさ調整（Master + 個別）
- 再生位置・長さ取得

#### Rust実装

```rust
pub struct PlayerPipeline {
    pipeline: gst::Pipeline,
    video_balances: HashMap<String, gst::Element>,  // output_id -> videobalance
}

impl PlayerPipeline {
    pub fn load(&self, path: &str) -> Result<()>;
    pub fn play(&self) -> Result<()>;
    pub fn pause(&self) -> Result<()>;
    pub fn seek(&self, position: f64) -> Result<()>;
    
    // 明るさ調整
    pub fn set_master_brightness(&self, value: f64) -> Result<()>;
    pub fn set_output_brightness(&self, output_id: &str, value: f64) -> Result<()>;
}
```

#### 成果物
- [ ] 単一ファイル再生
- [ ] 再生制御（Play/Pause/Stop/Seek）
- [ ] Master明るさ調整
- [ ] 出力別明るさ調整

---

### Phase 2: UI実装（2週間）

#### 機能
- Cueリスト表示・選択・編集
- 出力先管理
- プロジェクト保存/読込
- キーボードショートカット

#### UI構成

```
┌──────────────────────────────────┬────────────────────────────┐
│  Cue List                        │  Current Cue               │
│  ───────────────────────────     │  Items:                    │
│  1. Opening     (5 items)        │  🎵 audio → speaker        │
│▶ 2. Song 01     (5 items)        │  🎬 main  → Display 1      │
│  3. MC Bridge   (2 items)        │  🎬 side  → NDI            │
│                                  │                            │
│  [+ Add Cue]                     │  [+ Add Item]              │
├──────────────────────────────────┴────────────────────────────┤
│  Brightness                                                   │
│  Master: ════════●════ 80%                                    │
│  main:   ════════●════ 80%   [Link]                          │
│  side:   ══════●══════ 60%   [Link]                          │
├───────────────────────────────────────────────────────────────┤
│  [◀] [▶ PLAY] [■] [▶▶]                                       │
│  00:01:23 ════════●══════════════════════════════════ 04:12  │
└───────────────────────────────────────────────────────────────┘
```

- **Master**: 全出力に適用（Linkされた出力のみ）
- **個別**: 出力ごとに調整可能
- **Link**: Masterに連動するかどうか

#### キーボード

| キー | 機能 |
|------|------|
| Space | 再生/一時停止 |
| Enter | 選択Cue再生 |
| ↑/↓ | Cue選択 |
| ←/→ | 5秒シーク |

#### 成果物
- [ ] Cueリスト管理UI
- [ ] プロジェクト保存/読込
- [ ] キーボード操作

---

### Phase 3: マルチ出力（2週間）

#### 機能
- 複数出力ウィンドウ（Display/NDI）
- モニタ選択・フルスクリーン
- プレビューグリッド

#### 成果物
- [ ] 複数ウィンドウ管理
- [ ] モニタ一覧・選択
- [ ] フルスクリーン出力

---

### Phase 4a: NDI High Bandwidth（2週間）

#### 機能
- NDI送信（`ndisink`）
- NDI受信（`ndisrc`）
- ソース自動発見
- リージョン切り出し

#### GStreamer

```bash
# 送信
gst-launch-1.0 filesrc location=video.mp4 ! decodebin ! \
  videoconvert ! ndisink ndi-name="LivePlayer"

# 受信
gst-launch-1.0 ndisrc ndi-name="SOURCE" ! ndisrcdemux ! \
  videoconvert ! autovideosink
```

#### 成果物
- [ ] NDI送受信
- [ ] ソース一覧UI
- [ ] Receiver Mode

---

### Phase 4b: NDI|HX（2週間）※必要時

#### 機能
- H.264/H.265エンコード送信
- HWエンコーダ自動検出
- ビットレート設定

#### 注意
- NDI Advanced SDK必要
- 自前でエンコード→SDK送信

---

### Phase 5: マルチスクリーン（2週間）

#### 機能
- 複数映像を1ストリームに合成（2×2など）
- 1ストリームをリージョン分割

#### GStreamer

```bash
# 合成
gst-launch-1.0 compositor name=c \
  sink_0::xpos=0 sink_0::ypos=0 \
  sink_1::xpos=1920 sink_1::ypos=0 ! \
  ndisink ndi-name="MULTI" \
  filesrc location=a.mp4 ! decodebin ! c.sink_0 \
  filesrc location=b.mp4 ! decodebin ! c.sink_1
```

---

### Phase 6: TCチェイス（2週間）

#### 機能
- タイムコード表示
- NDI埋め込みTC読み取り
- 外部TCに追従再生

---

### Phase 7: リリース（1週間）

#### タスク
- GStreamerバンドル
- インストーラー作成（.msi / .dmg / .AppImage）
- GitHub Releases

---

## マイルストーン

| MS | 達成条件 | 目標 |
|----|---------|------|
| M0 | 環境構築完了、ASIOプラグイン動作 | 1週目 |
| M1 | GStreamer再生動作 | 3週目 |
| M2 | CueベースUI完成 | 5週目 |
| M3 | マルチ出力動作 | 7週目 |
| M4a | NDI送受信動作 | 9週目 |
| M4b | NDI\|HX動作（必要時） | 11週目 |
| M5 | マルチスクリーン動作 | 13週目 |
| M6 | TCチェイス動作 | 15週目 |
| M7 | リリースビルド完成 | 16週目 |

---

## リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| GStreamer学習コスト | 中 | チュートリアルから段階的に |
| **Windows ASIO** | **高** | **自前ビルド、WASAPIフォールバック** |
| ASIO SDKライセンス | 中 | Steinberg取得、再配布注意 |
| NDI Advanced SDK | 中 | まずHigh Bandwidthで開発 |
| HWエンコーダ環境差 | 中 | x264enc(CPU)フォールバック |
| Linux NDI\|HXデコード | 高 | 公式サポート限定 |
| マルチOS対応 | 中 | CI/CDで自動テスト |
| 長時間稼働 | 高 | Rust + GStreamerで信頼性確保 |

---

## 付録: 技術詳細

### A. プロジェクト構成

```
tauri-live-player/
├── src/                          # フロントエンド
│   ├── components/
│   │   ├── cue/                  # Cue関連
│   │   ├── output/               # 出力管理
│   │   ├── player/               # 再生制御
│   │   └── ui/                   # shadcn/ui
│   ├── stores/                   # Zustand
│   ├── types/
│   ├── App.tsx
│   └── main.tsx
│
├── src-tauri/
│   ├── src/
│   │   ├── commands/             # Tauriコマンド
│   │   ├── pipeline/             # GStreamer
│   │   ├── output/               # 出力管理
│   │   └── main.rs
│   └── Cargo.toml
│
├── package.json
└── README.md
```

### B. Cargo.toml

```toml
[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
serde = { version = "1", features = ["derive"] }
gstreamer = "0.23"
gstreamer-video = "0.23"
gstreamer-app = "0.23"
tokio = { version = "1", features = ["full"] }
anyhow = "1"
```

### C. オーディオドライバ対応

| OS | ドライバ | GStreamer | レイテンシ |
|----|---------|-----------|-----------|
| **Windows** | **ASIO** | `asiosink` | **~3ms** |
| Windows | WASAPI | `wasapisink` | ~10ms |
| macOS | Core Audio | `osxaudiosink` | ~10ms |
| Linux | JACK | `jackaudiosink` | ~5ms |
| Linux | ALSA | `alsasink` | ~20ms |

### D. HWエンコーダ対応

| OS | エンコーダ | GStreamer |
|----|-----------|-----------|
| Windows | NVIDIA | `nvh264enc` |
| Windows | Intel | `qsvh264enc` |
| Windows | AMD | `amfh264enc` |
| macOS | Apple | `vtenc_h264` |
| Linux | VA-API | `vaapih264enc` |
| 全OS | CPU | `x264enc` |

### E. CI/CD (GitHub Actions)

```yaml
name: Build
on: [push, pull_request]

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Install GStreamer
        # OS別インストール
      
      - uses: dtolnay/rust-toolchain@stable
      - uses: pnpm/action-setup@v2
      
      - run: pnpm install
      - run: pnpm tauri build
      
      - uses: actions/upload-artifact@v4
```

### F. Blackmagic Decklink 対応（参考）

GStreamer の gst-plugins-bad に Decklink プラグインが含まれており、
Blackmagic の SDI/HDMI キャプチャ・再生カードを直接制御可能。

#### エレメント一覧

| エレメント | 用途 |
|-----------|------|
| `decklinkvideosink` | SDI/HDMI 映像出力 |
| `decklinkaudiosink` | SDI/HDMI 音声出力 |
| `decklinkvideosrc` | SDI/HDMI キャプチャ |
| `decklinkaudiosrc` | 音声キャプチャ |

#### 使用例

```bash
# 映像出力
gst-launch-1.0 videotestsrc ! \
  decklinkvideosink device-number=0 mode=1080p25

# 映像+音声出力
gst-launch-1.0 \
  filesrc location=video.mp4 ! decodebin name=d \
  d. ! videoconvert ! decklinkvideosink device-number=0 mode=1080p25 \
  d. ! audioconvert ! decklinkaudiosink device-number=0
```

#### 主なプロパティ

| プロパティ | 説明 | 例 |
|-----------|------|-----|
| `device-number` | カード番号（0から） | `0`, `1` |
| `mode` | 解像度/フレームレート | `1080p25`, `1080p30`, `4Kp30` |
| `duplex-mode` | half / full | Duo2/Quad2用 |
| `connection` | 接続タイプ | `sdi`, `hdmi` |

#### 対応カード例

- DeckLink Mini Monitor / Recorder
- DeckLink SDI / HD Extreme
- DeckLink Duo2 / Quad2（複数系統）
- Intensity Pro（HDMI）
- UltraStudio

#### 注意

- Blackmagic Desktop Video ドライバのインストールが必要
- GStreamer公式インストーラーに含まれている（gst-plugins-bad）
- 将来的に出力タイプとして追加可能

#### 参考リンク

- [GStreamer Decklink Plugin](https://gstreamer.freedesktop.org/documentation/decklink/)

---

## 参考資料

- [GStreamer Documentation](https://gstreamer.freedesktop.org/documentation/)
- [gstreamer-rs](https://gitlab.freedesktop.org/gstreamer/gstreamer-rs)
- [gst-plugin-ndi](https://github.com/teltek/gst-plugin-ndi)
- [NDI SDK](https://ndi.video/for-developers/ndi-sdk/)
- [Tauri v2 Docs](https://v2.tauri.app/)
- [ASIO SDK](https://www.steinberg.net/developers/)

---

## 関連ドキュメント

- **[技術設計書 (TECHNICAL_DESIGN.md)](./TECHNICAL_DESIGN.md)** - 詳細な実装コード、型定義、パイプライン設計

---

## 次のアクション

1. [ ] macOS: GStreamerインストール、動作確認
2. [ ] Windows: GStreamer + ASIO SDKセットアップ
3. [ ] Tauriプロジェクト作成
4. [ ] `gst-launch-1.0` で動画再生テスト
5. [ ] gstreamer-rs Hello World

---

*最終更新: 2025-12-25*  
*バージョン: v5.2*  
*作成者: Claude + Kota*
